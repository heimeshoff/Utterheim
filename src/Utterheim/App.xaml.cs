using System.Windows;

namespace Utterheim;

/// <summary>
/// WPF Application stub. The actual host lifecycle (DI, hosted services,
/// tray, window) is owned by <see cref="EntryPoint"/> — App is just the XAML
/// boot vehicle WPF requires.
/// </summary>
public partial class App : Application
{
}
